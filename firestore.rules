rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Povolí komukoliv číst a zapisovat. Pouze pro testování!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}