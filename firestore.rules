rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Umožní čítanie a zápis každému prihlásenému používateľovi
      allow read, write: if request.auth != null;
    }
  }
}
